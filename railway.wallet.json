{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "services/wallet-service/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
